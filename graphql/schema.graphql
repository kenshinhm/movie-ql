type Movie{
    id: Int!
    title: String!
    rating: Float!
    summary: String!
    language: String!
    medium_cover_image: String!
}

type Query{
    movies(limit:Int, rating:Float): [Movie]!
    #    movie(id: Int!): Movie #! is not here because we might not find a person
}

#type Mutation {
#    addMovie(name:String!, score:Int!): Movie!
#    deleteMovie(id:Int!): Boolean!
#}